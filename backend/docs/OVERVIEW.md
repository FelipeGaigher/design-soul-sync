# TokenSync - Visão Geral do Sistema

## 🎯 Objetivo do Projeto

O **TokenSync** é uma plataforma que permite criar componentes no Figma e transformá-los automaticamente em código pronto para produção. O sistema atua como uma ponte entre designers e desenvolvedores, eliminando retrabalho e garantindo consistência visual.

## 🏗️ Arquitetura de Alto Nível

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                 USUÁRIOS                                     │
│                        (Designers & Desenvolvedores)                         │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
            ┌─────────────┐   ┌─────────────┐   ┌─────────────┐
            │   FIGMA     │   │  FRONTEND   │   │   CLI       │
            │  (Plugin)   │   │   (React)   │   │  (Futuro)   │
            │             │   │   :8080     │   │             │
            └─────────────┘   └─────────────┘   └─────────────┘
                    │                 │                 │
                    └─────────────────┼─────────────────┘
                                      │
                                      ▼
            ┌─────────────────────────────────────────────────┐
            │                    BACKEND                       │
            │                   (NestJS)                       │
            │                    :3000                         │
            │                                                  │
            │  ┌───────────┐  ┌───────────┐  ┌───────────┐   │
            │  │   Auth    │  │  Tokens   │  │Components │   │
            │  └───────────┘  └───────────┘  └───────────┘   │
            │  ┌───────────┐  ┌───────────┐  ┌───────────┐   │
            │  │  Figma    │  │   Code    │  │    AI     │   │
            │  │   Sync    │  │   Gen     │  │ Assistant │   │
            │  └───────────┘  └───────────┘  └───────────┘   │
            └─────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
            ┌─────────────┐   ┌─────────────┐   ┌─────────────┐
            │ PostgreSQL  │   │    Redis    │   │   MinIO     │
            │   (Data)    │   │   (Cache)   │   │ (Storage)   │
            └─────────────┘   └─────────────┘   └─────────────┘
                                      │
                                      ▼
            ┌─────────────────────────────────────────────────┐
            │              SERVIÇOS EXTERNOS                   │
            │                                                  │
            │  ┌───────────┐  ┌───────────┐  ┌───────────┐   │
            │  │ Figma API │  │ OpenAI    │  │  OAuth    │   │
            │  │           │  │ (GPT-4)   │  │ Providers │   │
            │  └───────────┘  └───────────┘  └───────────┘   │
            └─────────────────────────────────────────────────┘
```

## 🔄 Fluxo Principal: Figma → Código

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          FLUXO DE TRABALHO                                   │
└─────────────────────────────────────────────────────────────────────────────┘

1. DESIGN NO FIGMA
   ┌─────────────────┐
   │  Designer cria  │
   │  componentes e  │──────┐
   │  variáveis no   │      │
   │     Figma       │      │
   └─────────────────┘      │
                            │
2. CONEXÃO                  ▼
   ┌─────────────────┐      │
   │  Conectar       │      │
   │  projeto Figma  │◀─────┘
   │  ao TokenSync   │
   └─────────────────┘
            │
3. SINCRONIZAÇÃO    │
            ▼
   ┌─────────────────┐
   │  Importar       │     Detectar
   │  variáveis do   │────────────────┐
   │     Figma       │                │
   └─────────────────┘                │
            │                         │
            │                         ▼
4. DIVERGÊNCIAS     │        ┌─────────────────┐
            ▼                │   Divergência   │
   ┌─────────────────┐       │   Detectada?    │
   │  Resolver       │◀──────│                 │
   │  divergências   │  Sim  └─────────────────┘
   │  se houver      │                │ Não
   └─────────────────┘                │
            │                         │
            ├─────────────────────────┘
            │
5. GERENCIAMENTO    │
            ▼
   ┌─────────────────┐
   │  Organizar      │
   │  tokens e       │
   │  componentes    │
   └─────────────────┘
            │
6. GERAÇÃO          │
            ▼
   ┌─────────────────┐     ┌─────────────────────────────┐
   │  Exportar       │     │  FORMATOS DISPONÍVEIS:      │
   │  código         │────▶│  • JSON (Design Tokens)     │
   │                 │     │  • CSS Variables            │
   └─────────────────┘     │  • Tailwind Config          │
                           │  • React Components         │
                           │  • TypeScript Types         │
                           └─────────────────────────────┘
```

## 📊 Modelo de Domínio

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           MODELO DE DOMÍNIO                                  │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│    USER      │         │   PROJECT    │         │    TOKEN     │
├──────────────┤    1:N  ├──────────────┤    1:N  ├──────────────┤
│ id           │────────▶│ id           │────────▶│ id           │
│ email        │         │ name         │         │ name         │
│ name         │         │ description  │         │ value        │
│ figmaToken   │         │ figmaFileId  │         │ type         │
└──────────────┘         │ ownerId      │         │ category     │
                         └──────────────┘         │ figmaVarId   │
                                │                 └──────────────┘
                                │ 1:N
                                ▼
                         ┌──────────────┐
                         │  COMPONENT   │
                         ├──────────────┤
                         │ id           │
                         │ name         │
                         │ category     │         ┌──────────────┐
                         │ description  │    1:N  │   VARIANT    │
                         └──────────────┘────────▶├──────────────┤
                                                  │ id           │
                                                  │ name         │
                                                  │ tokens (JSON)│
                                                  └──────────────┘
```

## 🔌 Integrações

### Figma API
- **OAuth2**: Login e autorização
- **Variables API**: Leitura/escrita de variáveis
- **Components API**: Leitura de componentes
- **Webhooks**: Notificações em tempo real (futuro)

### OpenAI API
- **GPT-4**: Assistente IA para perguntas sobre Design System
- **GPT-4 Vision**: Análise de imagens de concorrentes (Benchmark)

## 📁 Estrutura de Módulos do Backend

```
src/modules/
├── auth/                  # Autenticação JWT + OAuth
│   ├── jwt.strategy.ts
│   ├── figma-oauth.strategy.ts
│   └── auth.service.ts
│
├── users/                 # Gerenciamento de usuários
│   └── users.service.ts
│
├── projects/              # CRUD de projetos
│   └── projects.service.ts
│
├── tokens/                # Design Tokens
│   ├── tokens.service.ts
│   └── token-history.service.ts
│
├── components/            # Biblioteca de componentes
│   ├── components.service.ts
│   └── variants.service.ts
│
├── figma/                 # Integração Figma
│   ├── figma.service.ts
│   ├── figma-sync.service.ts
│   └── figma-mapper.service.ts
│
├── code-generator/        # Geração de código
│   ├── code-generator.service.ts
│   └── generators/
│       ├── json.generator.ts
│       ├── css.generator.ts
│       ├── tailwind.generator.ts
│       ├── react.generator.ts
│       └── typescript.generator.ts
│
├── versioning/            # Versionamento
│   └── versioning.service.ts
│
├── scenarios/             # Cenários & Automação
│   ├── scenarios.service.ts
│   └── automation.service.ts
│
├── benchmark/             # Análise de concorrentes
│   ├── benchmark.service.ts
│   └── image-analysis.service.ts
│
└── ai-assistant/          # Chat IA
    └── ai-assistant.service.ts
```

## 🚀 Endpoints Principais

| Módulo | Método | Endpoint | Descrição |
|--------|--------|----------|-----------|
| Auth | POST | `/auth/login` | Login com email/senha |
| Auth | GET | `/auth/figma` | Iniciar OAuth Figma |
| Projects | GET | `/projects` | Listar projetos |
| Projects | POST | `/projects` | Criar projeto |
| Tokens | GET | `/projects/:id/tokens` | Listar tokens |
| Tokens | POST | `/projects/:id/tokens` | Criar token |
| Figma | POST | `/projects/:id/figma/sync` | Sincronizar Figma |
| Figma | GET | `/projects/:id/figma/divergences` | Ver divergências |
| Code | POST | `/projects/:id/code/export` | Exportar código |
| Code | GET | `/projects/:id/code/preview` | Preview de código |
| AI | POST | `/projects/:id/ai/chat` | Chat com IA |

## 🔒 Segurança

1. **Autenticação**: JWT com refresh tokens
2. **Rate Limiting**: 100 req/min por IP
3. **CORS**: Configurado para frontend
4. **Validação**: class-validator em todos os DTOs
5. **Sanitização**: Prevenção de XSS/SQL Injection

## 📈 Próximos Passos de Implementação

### Fase 1: Fundação (Semanas 1-2)
- [x] Estrutura do projeto NestJS
- [x] Configuração do Prisma + PostgreSQL
- [ ] Implementar módulo de autenticação
- [ ] CRUD básico de projetos

### Fase 2: Core Features (Semanas 3-4)
- [ ] CRUD de tokens
- [ ] CRUD de componentes
- [ ] Integração OAuth Figma
- [ ] Sincronização básica com Figma

### Fase 3: Geração de Código (Semanas 5-6)
- [ ] Gerador JSON
- [ ] Gerador CSS
- [ ] Gerador Tailwind
- [ ] Gerador React
- [ ] Gerador TypeScript

### Fase 4: Features Avançadas (Semanas 7-8)
- [ ] Detecção de divergências
- [ ] Versionamento
- [ ] Cenários
- [ ] Automações

### Fase 5: IA & Extras (Semanas 9-10)
- [ ] AI Assistant
- [ ] Benchmark de concorrentes
- [ ] WebSockets para real-time
- [ ] Plugin Figma (opcional)

---

**Documentação completa disponível em `backend/docs/`**
